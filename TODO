~~Create a id-kind-ip store for each node. It's a sqlite database.~~

Only `dir.rs` should access it directly.

~~Handle Spawn and Teach messages everywhere.~~
~~They're Identify messages now.~~

Fix all the binaries.

Use the Aux struct.

TEST.

Maybe make a CLI?